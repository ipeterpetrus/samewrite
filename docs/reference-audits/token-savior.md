# Reference audit: token-savior

Read-only audit of a depth-1 clone, 2026-09-14, by a subagent with path:line citations.
Coverage label is in the first line (FULL only where every non-binary tracked file was read).
Numeric claims quoted from the reference are EXTERNALLY_REPORTED — none was re-run by SameWrite.

```
REPO token-savior @ 73e9c7f56fd7cd40ecc72cdc4b032264018f8b4f license=MIT (LICENSE:1, "Copyright (c) 2025 Mibayy") lang=Python 3.11+ (396 .py / 31 .json / 10 .sh / 1 .js) files=473 read_fully=9 (README.md, CLAUDE.md, server.json, glama.json, 5 hooks/*-config.json) partial≈30 → SCOPED
Top-level: src/token_savior (173) · tests (225) · hooks (28) · scripts (18) · docs (7) · benchmarks (4) · bin/ts · .github/workflows (5). Paket PyPI `token-savior-recall` v4.21.0 (pyproject.toml:6-7).

WHAT: MCP server stdio (indeks simbol tree-sitter/regex + memory engine SQLite) PLUS 5 keluarga hook Claude Code, dipasang via `ts init` yang men-deep-merge ke settings.json agen (pyproject.toml:47-51; server.json:2-4; cli_init/__init__.py:2-3).

MECHANISMS (semua nyata di kode, bukan sekadar README):
1. MCP server: `token-savior` = server.py `main_sync` (pyproject:48). Manifest dibatasi profil `TOKEN_SAVIOR_PROFILE` optimized/tiny/lean/full/compact-only (server.json:34-38). Menulis `<repo>/.token-savior-cache.json` (cache_ops.py:43,54) dan `<repo>/.token-savior-checkpoints/` (checkpoint_ops.py:163) ke root proyek pengguna; membaca `<repo>/.token-savior/hint.md` (server_handlers/project.py:36).
2. PreToolUse Bash REWRITER: hooks/bash_rewriter_hook.py:67-117 — emit `hookSpecificOutput.updatedInput.command` (mis. `git status`→`--porcelain=v2 --branch`); 12 rule (git status/diff/log, tsc, pytest, npm test, cargo test, gh run watch, gh pr list, docker ps, grep, cat) di src/token_savior/bash_rewriter/rules.py:110-330; skip bila ada `| > < && || ; $( \``, `--`, atau flag verbose (rules.py:26-55). OFF default (`TS_BASH_REWRITE=1`), audit log opsional JSONL (:55-60). Ini satu-satunya lapis yang mengecilkan giliran BERJALAN (README "Which of those two actually shrinks a turn"; read_guard_hook.py:4-9).
3. PreToolUse READ GUARD: hooks/read_guard_hook.py:109-188 — Read tanpa `limit`/`offset` pada berkas kode >600 baris → inject `limit`=400 + alasan; tak pernah deny; ON default (`TS_READ_GUARD=0` mematikan, :34). TIDAK termasuk bundle `ts init` (agent_paths.py:148-158) — harus di-merge manual.
4. PreToolUse DISCIPLINE GUARD: hooks/ts_discipline_guard.py:439-480 — `permissionDecision: deny` untuk Read/Edit/Write/Grep/shell native pada source ter-indeks (.py/.ts/.tsx/.js/.jsx, :52). Opt-in `TS_DISCIPLINE_GUARD=1`, escape `TS_GUARD_OFF=1` (:40-43); fail-open. ADA di bundle claude (agent_paths.py:153) — terpasang tapi inert.
5. PostToolUse TOOL CAPTURE + 34 COMPACTOR: hooks/tool_capture_hook.py:62-215 — output >4096 B disimpan ke tabel SQLite `tool_captures`, emit `additionalContext` "ts://capture/{id}"; `TS_BASH_COMPACT=1` menjalankan registry compactors/__init__.py. ADITIF SAJA — README sendiri: "These run in PostToolUse, so they do not shrink the current turn."
6. MEMORY ENGINE 7 event (memory-hooks-config.json:6-73: SessionStart/UserPromptSubmit/PreToolUse/PostToolUse/PreCompact/Stop/SessionEnd; Stop timeout 120000 ms): inject indeks memory + baris "[mem:N obs · mode:X]" ke stdout (memory-session-start.sh:141-175, 302-337); top-3 obs per prompt (memory-userprompt.sh:1-4); memory per simbol pre-tool (memory-pretooluse.sh:138-161); auto-capture bash sukses → `~/.config/token-savior/activity_tracker.json` (memory-posttooluse.sh:185-222); ringkasan Stop/End + Telegram push (memory-session-stop.sh:1-6). Ranking LinUCB bandit (linucb_injector.py:1-14 → server_state.py:19), prefetch Markov/PPM (markov_prefetcher.py → server_state.py:20), distilasi MDL (mdl_distiller.py → memory/distillation.py:31).
7. hooks/rules-enforce.sh:1-4 + ledger-rules.json: PreToolUse deny force-push main / blanket DELETE. Dikirim, TIDAK di bundle.
8. `ts init`: cli_init/__init__.py:262-372 — deteksi agen, load bundle, merge, backup `.bak-YYYYMMDD-HHMMSS`, tulis atomik (:92-96, :361-372).
9. `ts` CLI + daemon `/tmp/ts.sock` (bin/ts, cli.py) untuk agen non-MCP.

FOREIGN MUTATION:
- YA, settings agen: `~/.claude/settings.json` atau `<cwd>/.claude/settings.json`, `~/.cursor/settings.json`, `~/.gemini/settings.json`, `~/.codex/hooks.json`, `~/.openclaw/openclaw.json` (agent_paths.py:39-69). Sifat: deep-merge ADITIF kunci `hooks` saja; entri asing dipertahankan, dedup fingerprint (matcher, command) (merger.py:39-66, 75-103); guard duplikat lintas-scope global↔proyek (#99, cli_init/__init__.py:310-317). Tidak ada kode yang menghapus/menimpa hook milik plugin lain.
- TIDAK menulis `statusLine` di settings.json — "statusline" hanya teks yang di-echo ke stdout SessionStart (memory-session-start.sh:302-337). TIDAK menulis CLAUDE.md/AGENTS.md (hanya baca hint; server_handlers/analysis.py:89 komentar). OpenClaw: memutasi `context.bootstrapFiles` (handler.js:85-87), sengaja tak memakai nama AGENTS.md.
- Menulis ke repo pengguna: `.token-savior-cache.json`, `.token-savior-checkpoints/` (self-exclude dari indeks project_indexer.py:25,268; nol bukti ditambah ke .gitignore). Home: `~/.local/share/token-savior/` (memory.db, linucb_model.json, last_injected_state.json), `~/.config/token-savior/`, `~/.cache/token-savior/interpreteur`, `~/.local/state/token-savior/hook-errors.log` (memory-session-start.sh:13,29,62).

CLAIMS [EXTERNALLY_REPORTED, semua]:
- README.md:6 "**97.9% on tsbench at -80% tokens.**"; :22-24 "188 / 192 (97.9%)" · "3 395 (-80%)" · "18.9 s (-83%)"; pyproject.toml:7 "97.9% accuracy at −80% tokens on tsbench"; glama.json:4 "97% token savings on code navigation"; README ~:302 "97% fewer chars injected across 170+ real sessions"; tabel ~:279-286 "-99.9%", "~20.4 K/week".
- README SENDIRI mencabut validitasnya: :25-27 "its repository is not public at the moment, so take the figures as reported rather than as independently verifiable"; :29-38 re-ukur 9-Agu ditarik — "across 143 benchmark sessions, exactly one called a Token Savior tool"; :47-48 "stand as reported and unverified". CLAUDE.md:52-56 mengaku 4 poin terakhir butuh prompt sistem khusus.
- In-repo measurement: benchmarks/run_benchmarks.py = kecepatan indeks/query fastapi+cpython, BUKAN token (:1-4, :25-28); benchmarks/tool_capture_bench.py = payload SINTETIS yang ukurannya "mirror the Context-mode README claims" (:24-26) → sirkular, hasil 968 B tetap per skenario (tool_capture_results.json:7-8); scripts/bench_compactors_real.py replay transkrip lokal `~/.claude/projects` (:1-6); scripts/audit_budget.py distribusi budget (:13-19, aproksimasi char/4 :23-25); scripts/check_bench_gates.py gate MRR retrieval (:27-40), bukan token. Tsbench harness ABSEN dari repo. Scorer TIDAK di-self-test: `grep -rln bench|audit_budget|check_bench_gates tests/` = 0 berkas.

OWNED IDENTIFIERS: perintah `ts`, `token-savior`, `token-savior-dashboard`, `token-savior-bench` (pyproject:47-51); nama MCP `token-savior`/`token-savior-recall`, prefiks tool `mcp__token-savior[\w-]*__`; ~50 env `TS_*`/`TOKEN_SAVIOR_*` (TS_BASH_REWRITE, TS_BASH_COMPACT, TS_READ_GUARD, TS_DISCIPLINE_GUARD, TS_GUARD_OFF, TS_MEMORY_DISABLE, TS_CAPTURE_DISABLED, TS_RULES_DISABLE, TOKEN_SAVIOR_PROFILE/CLIENT/DATA_DIR/STATS_DIR, dst); URI `ts://capture/{id}`, `ts://obs/{id}`; state di §FOREIGN. Frasa pemicu prosa: NOL — grep "normal mode|stop ponytail|caveman|terse|verbose mode" di src/hooks/scripts/bin = 0 hit selain komentar server.py:819 dan tabel README:272. Semuanya event-driven, bukan kata-kunci prompt. (memory-userprompt.sh:4 menyebut "trigger phrases" untuk memory — daftarnya TIDAK kubaca.)

NOVELTY VERDICT: SATU mekanisme berbeda-lever = PreToolUse `updatedInput` (rewriter #2 + read-guard #3): mengubah INPUT tool sebelum eksekusi, bukan menyaring output (rtk-per-framing-induk), bukan navigasi LSP (serena — TS mengaku overlap langsung, README:270 "Run TS as compact-only if serena is your navigator"), bukan teks instruksi (ponytail/caveman; CLAUDE.md:22-40 milik TS sendiri = instruction-skill duplikat). Sisanya DUPLIKAT: compactors ≙ rtk (PostToolUse, TS mengakui "same layer"), symbol index ≙ serena, memory engine ≙ claude-mem, CLAUDE.md tabel routing ≙ instruction-skill. Catatan jujur: bila rtk sebenarnya juga menulis ulang perintah pre-exec lewat hook (klaim TS README:268 bahwa rtk = PostToolUse TIDAK kuverifikasi di sini), maka lever ini pun bukan baru — bedanya hanya edit flag in-place vs prefiks binari pembungkus.

BORROW CANDIDATES:
1. Inject `limit` pada Read tak berbatas (stdlib, fail-open, never-deny) → hooks/read_guard_hook.py:109-188
2. Guard komposisi-shell sebelum rewrite (pipe/redirect/`--`/flag verbose → pass-through) → src/token_savior/bash_rewriter/rules.py:26-55
3. Dedup hook lintas-scope saat merge settings (global+proyek = fire 2x) → src/token_savior/cli_init/__init__.py:310-317 + merger.py:39-66```
