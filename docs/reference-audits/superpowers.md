# Reference audit: superpowers

Read-only audit of a depth-1 clone, 2026-09-14, by a subagent with path:line citations.
Coverage label is in the first line (FULL only where every non-binary tracked file was read).
Numeric claims quoted from the reference are EXTERNALLY_REPORTED — none was re-run by SameWrite.

```
REPO superpowers @ b36e0829c6d0140e93cfef2ca599b1b07d4a7797 license=MIT (LICENSE:1, © 2025 Jesse Vincent) files=195 read_fully=17 → SCOPED
root=/tmp/claude-1001/-home-ubuntu/9203b681-5613-4b54-a54e-7b0dd5c1e86c/scratchpad/refs/superpowers ; v6.3.0 (.claude-plugin/plugin.json:4, RELEASE-NOTES.md:3 dated 2026-08-12)
top-level: tests/60 skills/51 docs/43 .github/6 scripts/4 hooks/4 + 8 per-harness manifests (.codex-plugin .cursor-plugin .devin-plugin .kimi-plugin .hermes-plugin .opencode .pi gemini-extension.json). NO commands/, NO agents/ dir.

LOADING:
- One hook only: SessionStart, matcher "startup|clear|compact", sync (hooks/hooks.json:4-10) → run-hook.cmd (bash/cmd polyglot, hooks/run-hook.cmd:1-46) → hooks/session-start.
- session-start does `cat skills/using-superpowers/SKILL.md` (hooks/session-start:11), JSON-escapes it (:16-24), wraps in `<EXTREMELY_IMPORTANT>You have superpowers... For all other skills, use the 'Skill' tool` (:27), emits hookSpecificOutput.additionalContext for Claude Code / additional_context for Cursor / additionalContext otherwise (:38-46). Read-only; exit 0.
- Always-on injected text = 3108 B SKILL.md + 225 B wrapper ≈ 3.3 KB (~800 tok), re-injected after every /clear and /compact (matcher).
- Skill discovery = ZERO custom loader: relies on native plugin `skills/*/SKILL.md` frontmatter listing in the Skill tool; the 13 other skills stay on disk until invoked. Descriptions are the triggers (e.g. brainstorming "You MUST use this before any creative work" skills/brainstorming/SKILL.md:3).
- Bootstrap skill = using-superpowers. Core rule: "If you think there is even a 1% chance a skill might apply ... you ABSOLUTELY MUST invoke the skill" (skills/using-superpowers/SKILL.md:11) + "Invoke relevant or requested skills BEFORE any response or action — including clarifying questions, exploring the codebase" (:20); announce "Using [skill] to [purpose]" (:24); process skills before implementation skills (:28-31); 12-row "Red Flags" rationalization table (:37-50); `<SUBAGENT-STOP>` subagents ignore it (:6-8); precedence "User instructions (CLAUDE.md, AGENTS.md...) take precedence over skills" (:63).
- OpenCode variant injects bootstrap into FIRST USER MESSAGE, not system prompt, to avoid per-turn token bloat (.opencode/plugins/superpowers.js:115-118,133-136).

SKILLS (4):
- verification-before-completion (3.6 KB, read fully): Iron Law "NO COMPLETION CLAIMS WITHOUT FRESH VERIFICATION EVIDENCE" (:17); 5-step gate identify→run→read→verify→claim, "Skip any step = lying" (:25-35); claim/evidence table incl. "Agent completed → VCS diff shows changes, not agent report" (:40-48); banned words should/probably/seems, "Great!/Done!" before verification (:52-59). Verify-the-verifier: regression test = "Write → Run (pass) → Revert fix → Run (MUST FAIL) → Restore → Run (pass)" (:84); "Agent reports success → Check VCS diff → Verify" (:102). No N-counter.
- systematic-debugging (9.5 KB, read fully): Iron Law "NO FIXES WITHOUT ROOT CAUSE INVESTIGATION FIRST" (:17); 4 phases: root-cause (read errors, reproduce, recent changes, instrument each component boundary, trace data flow) → pattern → single hypothesis/minimal change → fix with failing test first (:48-189). STOP CONTROL: "If Fix Doesn't Work → Count fixes; <3 return to Phase 1; ≥3 STOP and question the architecture ... DON'T attempt Fix #4 without architectural discussion ... Discuss with your human partner" (:191-212). Human-signal phrases "Stop guessing"/"Ultra-think this"/"We're stuck?" → return to Phase 1 (:233-242). Refs root-cause-tracing.md, defense-in-depth.md, condition-based-waiting.md, find-polluter.sh.
- test-driven-development (9 KB, read fully): "NO PRODUCTION CODE WITHOUT A FAILING TEST FIRST ... Write code before the test? Delete it" (:34-45); Verify RED mandatory: fails (not errors), for the expected reason; "Test passes? You're testing existing behavior" (:113-128); verify GREEN incl. pristine output (:168-183); 11-row rationalization table (:212-226); checklist "Can't check all boxes? You skipped TDD. Start over" (:283-296); exceptions only with human permission (:24-27,:320).
- brainstorming (15.5 KB, read fully): `<HARD-GATE>` no code/scaffold/implementation skill until human approved intent, "the ceremony scales with the task; the approval gate never does" (:14-20); classify aloud spike/bounded/architectural, "when in doubt take the heavier one", one-way ratchet (:22-52); one question per message (:171); 2-3 approaches (:98); spec → docs/superpowers/specs/YYYY-MM-DD-<topic>-design.md, self-review for TBD/contradiction/scope/ambiguity (:100-101,:211-219), then ONLY writing-plans (:149-154). Optional browser "visual companion" offered just-in-time (:233-250).

SUBAGENTS:
- dispatching-parallel-agents (read fully): "They should never inherit your session's context or history — you construct exactly what they need" (:10); one agent per independent domain, dispatch in one response = parallel (:66-77); prompt = focused/self-contained/output-specified (:87-113); post-return verify: conflicts, full suite, spot-check (:161-167).
- subagent-driven-development (32 KB, skimmed): fresh implementer per task + task review + final review (:8-12); "Everything you paste into a dispatch prompt — and everything a subagent prints back — stays resident in your context ... Hand artifacts over as files" (:231-233); model selection cheapest-that-works, "Always specify the model explicitly" (:184-219); fix loop cap "R of 5: R≤3 resume implementer; R≥4 fresh implementer, more capable model" (:75,:201-202); only 4 stop reasons (irreversible, security, out-of-worktree side effect, plan hopeless) (:27-31); plan-scoped ledger `.superpowers/sdd/<plan>/progress.md` with identity first line, trusted over memory after compaction (:141-151); prompts in implementer-prompt.md / task-reviewer-prompt.md / re-review-prompt.md.
- Helper scripts (read fully): scripts/task-brief extracts one "## Task N" from plan to a file "so the task text never has to be pasted through the controller's context" (:2-4,:28-34); scripts/review-package writes commits+stat+`git diff -U10 BASE..HEAD` to a file (:113-124); scripts/sdd-workspace = `<git root>/.superpowers/sdd/<plan-basename>/` + self-ignoring .gitignore, in-tree because Claude Code denies writes under .git/ (:52-56,:76-80).

OWNED IDENTIFIERS:
- Skill namespace superpowers:{using-superpowers, brainstorming, writing-plans, executing-plans, subagent-driven-development, dispatching-parallel-agents, test-driven-development, systematic-debugging, verification-before-completion, requesting-code-review, receiving-code-review, using-git-worktrees, finishing-a-development-branch, writing-skills}. No slash commands, no agents.
- Markers: `<EXTREMELY_IMPORTANT>`, `<SUBAGENT-STOP>`, `<HARD-GATE>`, announce "Using [skill] to [purpose]", "your human partner" (deliberate voice, CLAUDE.md "Understand the Project").
- Env: reads CLAUDE_PLUGIN_ROOT / CURSOR_PLUGIN_ROOT / COPILOT_CLI (hooks/session-start:38-45); SUPERPOWERS_DISABLE_TELEMETRY, SUPERPOWERS_VERSION, SUPERPOWERS_BRAND_IMAGE_URL, honors DISABLE_TELEMETRY + CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC (skills/brainstorming/scripts/server.cjs:105-112); BRAINSTORM_PORT_FILE (start-server.sh:120).
- State: `<repo>/.superpowers/sdd/...`, `<repo>/.superpowers/brainstorm/<id>/state/{server.pid,server-instance-id,server-info,server.log}` or `/tmp/brainstorm-<id>` (start-server.sh:117-129); docs/superpowers/{specs,plans}/; `.worktrees/` (.gitignore:1). Brainstorm server binds 127.0.0.1 (start-server.sh:26), loads logo from primeradiant.com with version = telemetry (server.cjs:106; README.md:346).
- Generic phrases: NONE — grep for "normal mode"/"stop superpowers"/"/superpowers" = 0 hits in skills/hooks; no on/off toggle exists at all.

FOREIGN MUTATION: none. grep settings.json|statusLine|installed_plugins|~/.claude across hooks/scripts/skills/manifests → only doc mentions (skills/writing-skills/SKILL.md:12, CREATION-LOG.md:7). Hook is read+print (session-start:11,40-46). Writes limited to user repo (.superpowers/, docs/superpowers/, git commits/worktrees per skill text) and /tmp. scripts/*.sh are maintainer release tooling (bump-version, lint-shell, package/sync codex plugin), not runtime.

CLAIMS: README numeric = only "It's 100% optional" (telemetry, README.md:346); no perf/token numbers. CLAUDE.md:7,54 "This repo has a 94% PR rejection rate" [EXTERNALLY_REPORTED]. docs/superpowers/specs/2026-07-06-sdd-plan-scoped-workspace-eval-results.md:45,149-152 "25/25 controller reps refused ... 15/15 refused adoption ... 5/5" [EXTERNALLY_REPORTED, self-authored eval]. Benchmark dir: none tracked — evals/ gitignored (.gitignore:12-15), lives in github.com/prime-radiant-inc/superpowers-evals (CLAUDE.md §Eval harness: tmux-driven Claude Code/Codex/Gemini + LLM verifier). tests/ = plugin-infra tests (hook JSON shape tests/hooks/test-session-start.sh:33-70; skill-trigger judge = `"name":"Skill"` present AND no other tool_use before it, tests/explicit-skill-requests/run-test.sh:81-112; tests/claude-code/analyze-token-usage.py per-subagent token split).

COLLISION RISKS / COMPOSE NOTES:
- Bootstrap is maximalist ("1% chance → MUST invoke", skill check "BEFORE clarifying questions") and the brainstorming HARD-GATE + systematic-debugging "discuss with human after 3 fixes" both mandate human approval — direct tension with any autonomous/DO-IT-DON'T-ASK doctrine; superpowers itself yields to CLAUDE.md (using-superpowers:63), so a sibling's CLAUDE.md rule wins by its own terms.
- SessionStart additionalContext is additive; ~3.3 KB re-added on every compact/clear — budget it. Both plugins' SessionStart hooks run; no ordering guarantee.
- Skill-name overlap: if samewrite ships a "brainstorming"/"tdd"/"debugging" skill, the Skill tool lists both; superpowers routes by `superpowers:` prefix, so a distinct prefix avoids ambiguity. OpenCode duplicate-name behavior is documented as unresolved (tests/opencode/test-priority.sh:2-5).
- `.superpowers/` and `docs/superpowers/` paths are theirs; do not reuse. No mode toggle exists, so a sibling "normal mode" phrase cannot collide with superpowers.

BORROW CANDIDATES (mechanism → path) — max 5
1. 3 KB router-only SessionStart injection; everything else lazy via native Skill tool → hooks/session-start:11,27 + hooks/hooks.json:4
2. Revert-fix-MUST-FAIL regression check + "agent said success → check VCS diff" → skills/verification-before-completion/SKILL.md:84,100-103
3. Failed-fix counter: <3 re-investigate, ≥3 stop/architecture; 5-round cap with model escalation at R≥4 → skills/systematic-debugging/SKILL.md:191-212; skills/subagent-driven-development/SKILL.md:75,201-202
4. File-handoff to subagents (brief/diff written to plan-scoped workspace, never pasted) + identity-lined ledger trusted over memory post-compaction → skills/subagent-driven-development/scripts/{task-brief,review-package,sdd-workspace}; SKILL.md:141-151,231-233
5. Skill-behaviour eval: RED baseline without skill, PASS = Skill tool fired before any other tool → skills/writing-skills/testing-skills-with-subagents.md:34-39; tests/explicit-skill-requests/run-test.sh:81-112

Coverage: read fully 17/195 (hooks.json, hooks-cursor.json, session-start, run-hook.cmd, plugin.json, marketplace.json, using-superpowers, verification-before-completion, systematic-debugging, test-driven-development, brainstorming, dispatching-parallel-agents SKILL.md, task-brief, sdd-workspace, review-package, CLAUDE.md, .gitignore); partial ~16 (README, SDD SKILL.md, writing-skills + testing doc, start-server.sh, server.cjs, visual-companion.md, executing-plans, receiving-code-review, writing-plans, tests/*, eval-results spec, .opencode plugin, AGENTS.md, RELEASE-NOTES); rest listed only.```
