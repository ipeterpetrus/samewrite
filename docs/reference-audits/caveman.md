# Reference audit: caveman

Read-only audit of a depth-1 clone, 2026-09-14, by a subagent with path:line citations.
Coverage label is in the first line (FULL only where every non-binary tracked file was read).
Numeric claims quoted from the reference are EXTERNALLY_REPORTED — none was re-run by SameWrite.

```
REPO caveman @ 15581d14007fd01fb3f132016741962f34936ca2 license=MIT (LICENSE:1-3 scope note: MIT except engine-linked dirs engine/ proxy/ rewriter/ browse/ mcp/ shrink/ shared/platform/ → LICENSE.BSL) files=1388 read_fully=18 → SCOPED
Top-level: packages 328 · proxy 260 · engine 236 · shared 94 · tests 64 · skills 59 · extension 46 · docs 44 · src 32 · root 28 · browse 25 · mcp 23 · agents 23 · plugins 20 · mem 19 · shrink 16 · integrations 15 · .github 11 · rewriter 10 · bin 7 · scripts/evals/commands 6 each · benchmarks 5 · .codex/.claude-plugin 2 · dist 1. Most of the tree (packages/proxy/engine/browse/mcp/extension) is the cloud/proxy product, not the plugin.

SKILL: skills/caveman/SKILL.md 7022 B; byte-identical copy plugins/caveman/skills/caveman/SKILL.md (cmp IDENTICAL; enforced by tests/verify_repo.py:177 verify_synced_files). Levels lite/full/ultra/wenyan-lite/wenyan-full/wenyan-ultra (SKILL.md:42-49), default full (:15). Rules (:19-33): drop articles/filler/pleasantries/hedging; fragments OK; short synonyms; NO invented abbreviations (cfg/impl) and NO arrows — "zero token saved"; never drop not/never/no/only; never ADD words to sound caveman; STE clarity register (≤20-word sentences, one term one meaning, imperative); no tool-call narration; keep user's language; code/errors verbatim. Auto-Clarity (:68-77): drop mode for security warnings, irreversible confirmations, order-sensitive multi-step. Boundaries (:90): persisted text (code, commits, PR/issue bodies, memory files) = normal prose. Runtime injects SKILL.md filtered to ONE level's table row + examples (src/hooks/caveman-config.js:680-719) under banner `CAVEMAN MODE ACTIVE — level: X` (:723-725); hardcoded fallback ruleset if SKILL.md unreadable (caveman-activate.js:366-383). Tiny IDE rule variant src/rules/caveman-activate.md (645 B). NOTE: skills/native-core.md (1667 B) is a separate "build simplest complete system" ladder (ponytail-like), compiled into skills/generated/<target>/pack.json — not the prose skill.

TRIGGERS (src/hooks/caveman-parse.js; regex over lowercased, whitespace-collapsed prompt; quoted spans "…"/`…` blanked first :72,:227; NL matching skipped when prompt starts with '/' or carries a foreign <command-name> envelope :226, tracker:185-193):
 OFF (:246-256, checked FIRST, negation NOT applied by design :102-107): `\b(stop|disable|deactivate|quit|exit|kill)\s+(the\s+)?caveman\b` · `\bcaveman(\s+mode)?\s+(off|stop|disabled?)\b` · `\bturn\s+off\s+(the\s+)?caveman\b` · `^(please\s+)?(go\s+|back\s+to\s+|switch\s+(back\s+)?to\s+|return\s+to\s+)?normal\s+mode\b` · `normal mode` anywhere AND `caveman` anywhere.
 ON (:93-144; per-clause negation guard :121-132; question guard :266-267): `\b(activate|enable|start|turn on|use|switch to|want|give me)\b` + whitelist gap (the/to/into/on/in/please/now/it/me/my/this/that/mode) + `caveman` · `talk like (a|an|the)? caveman` · `caveman mode (on|please|now)` · `^caveman( mode)?[.!]*$` · BREVITY `\b(less tokens|fewer tokens|be brief|be terse|shorter answers)\b` unless followed by in/for/on/about/when/during/with (:137).
 SLASH (:288-306): `/caveman [lite|full|ultra|wenyan-*|off|stop|disable]`, namespaced `/caveman:caveman`, `/caveman-commit|review|compress` (one-shot independent modes, auto-restored next turn: caveman-mode-tracker.js:328-346). Bogus level → "not recognized" notice, level unchanged (:189-192). `<scheduled-task` prompts → full bail (tracker:168). Corpus: tests/fixtures/mode-activation/cases.json (5379 B, fields id/prompt/expected/issue/category/known_failing) + tests/test_mode_activation_corpus.js.

HOOKS (.claude-plugin/plugin.json:9-32; both `node "$HOOK_ROOT/src/hooks/…"` timeout 30):
 SessionStart | caveman-activate.js | stdout = banner + level-filtered SKILL.md, plus one-shot "STATUSLINE SETUP NEEDED" nudge (:413-420) | reads payload source/cwd/session_id; RESET_SOURCES={startup,clear} re-derive default (:220), compact/resume/fork read stored literal (:292-307); writes $CLAUDE_CONFIG_DIR/.caveman-sessions/<sid>.mode + legacy .caveman-active mirror, .caveman-mode-log.jsonl, .caveman-nudge-shown (:389); reads settings.json only to probe statusLine (:392-402); GC session files >14d (config:543-572).
 UserPromptSubmit | caveman-mode-tracker.js | hookSpecificOutput.additionalContext = [unresolved-level notice, full ruleset on level SWITCH, per-turn reinforcement `CAVEMAN MODE ACTIVE (<mode>). Enforce this reply: …`] (:111-125, :383-390); `/caveman-stats` spawns caveman-stats.js (2.5 s) and injects its block (:200-234) | writes .caveman-sessions/<sid>.mode/.prev, .caveman-active(.prev), .caveman-mode-log.jsonl.
 statusLine (settings.json slot, not a hook) | caveman-statusline.sh/.ps1 | reads session_id from stdin JSON, badge [CAVEMAN]/[CAVEMAN:ULTRA] (:70-75) + savings suffix from .caveman-statusline-suffix (:84-90); exits 0 always (:94).
 caveman-stats.js | reads newest ~/.claude/projects/**/*.jsonl transcript (:141-156), .caveman-history.jsonl (:610), writes .caveman-statusline-suffix (:686).
 Config resolution (config:14-26,118-138): CAVEMAN_DEFAULT_MODE → cwd-walk `.caveman/config.json` | `.caveman.json` (defaultMode) → $XDG_CONFIG_HOME/caveman/config.json | ~/.config/caveman/config.json | %APPDATA%\caveman\config.json → 'full'. Env: CLAUDE_CONFIG_DIR, CLAUDE_PLUGIN_ROOT (config:665), CAVEMAN_DEFAULT_MODE, CAVEMAN_DEBUG, CAVEMAN_SESSION_TTL_MS, CAVEMAN_STATUSLINE_SAVINGS. Flag IO: 0600, O_NOFOLLOW, temp+rename, 64-byte cap, VALID_MODES whitelist (config:168-319).

COMMANDS: commands/*.toml (Gemini form) caveman · caveman-commit · caveman-review · caveman-stats · caveman-init (+ commands/caveman-init.md Claude form). skills/: caveman, caveman-commit, caveman-compress, caveman-help, caveman-review, caveman-stats, cavecrew, caveman-discover/explore/learn/manage/optimize/setup/evidence-review, investigate-first, lean-build, migration, safe-refactor, surgical-patch, verify-and-stop. Agents cavecrew-builder/investigator/reviewer via default agents/ scan — plugin.json must NOT declare `agents` (loads zero; verify_repo.py:254-266).

STATUSLINE/CONFIG: standalone installer bin/install.js:1199-1211 — no statusLine → set caveman; existing caveman → "already configured"; existing FOREIGN → NOT added, prints NOTE pointing to src/hooks/README.md snippet. bin/lib/settings.js: JSONC-tolerant read, null on hard failure never `{}` overwrite (:103-122); atomic 0600 write (:126-137); ownership = exact basename ∈ MANAGED_HOOK_BASENAMES, never substring "caveman" (:195-227); foreign hooks preserved (:143-159,:233-256); orphan prune incl. stale statusLine (:307-374). Plugin path never writes settings.json (nudge only).

ADAPTERS: Claude Code plugin (.claude-plugin) · Codex (.codex/hooks.json = SessionStart `echo 'CAVEMAN MODE ACTIVE…'` one-liner; plugins/caveman/.codex-plugin/plugin.json v0.1.0) · Gemini (gemini-extension.json + commands/*.toml) · opencode (src/plugins/opencode/plugin.js evaluates caveman-config.js via new Function — config:4-12; shares caveman-parse.js) · Hermes/OpenClaw/aider/pi (packages/, bin/lib/openclaw.js) · IDE rule files Cursor/Windsurf/Cline/Copilot/AGENTS.md via src/tools/caveman-init.js (commands/caveman-init.md:6). Generator skills/compile.mjs: registry.json + native-core.md + SKILL.md → skills/generated/{claude,codex,hermes,gemini,opencode,aider}/pack.json + packages/cli/src/*.generated.ts (:254-276), byte-budget validated (:212). Drift: verify_repo.py verify_synced_files byte-compares copies (:177-236) + hook checksums.sha256 (:313-319); agents/compile.mjs fails closed on pin mismatch (tests/installer/agent-registry-compile.test.mjs:62-77). No CI `git diff --exit-code` over skills/generated found (only sync-skill.yml:75). Version-consistency test: NONE — package.json 2.6.0, registry native_pack 2.2.0, codex plugin.json 0.1.0, .claude-plugin/plugin.json has no version field; grep "version" tests/verify_repo.py = 0 hits.

BENCHMARK: benchmarks/run.py (anthropic SDK, usage.output_tokens :94, --dry-run, rewrites README region :267) + evals/ (claude -p, 3 arms baseline/"Answer concisely."/skill; tiktoken o200k_base; committed snapshot; honest delta = skill vs terse arm — evals/README.md:15-20, measure.py:9-13). README claims [EXTERNALLY_REPORTED]: "output tokens saved (skill) 65%", "input tokens saved (proxy) 33%" (README.md:68-69); avg 1214→294 = 65% over 10 prompts (:154,:176); caveat :182 "skill's own rules cost about 1 to 1.5k input tokens every turn… you can lose money"; proxy −33.2% total, one case +9.9% (:197-198); browse 129.8× (:204); skill-as-PNG 1069→415 61% (:292). Scorer self-test: tests/test_benchmark_contract.py = contract test (dry-run model, one README region, no uncommitted % strings :17-40) — NOT a scorer-accuracy self-test.

COLLISION RISKS for a sibling plugin:
- Phrases: prompt-initial "normal mode" deactivates caveman regardless (parse.js:236) — ponytail (loaded here) also uses "stop ponytail"/"normal mode"; a third "normal mode" owner = triple fire. "be brief/be terse/less tokens/fewer tokens/shorter answers" ACTIVATE caveman (parse.js:137) — samewrite must not use brevity words as its own triggers. Safe: "<verb> samewrite" needs the word 'caveman' to collide.
- State paths (all $CLAUDE_CONFIG_DIR): .caveman-active, .caveman-active.prev, .caveman-sessions/, .caveman-mode-log.jsonl, .caveman-history.jsonl, .caveman-statusline-suffix, .caveman-nudge-shown; repo .caveman.json / .caveman/config.json; ~/.config/caveman/; ~/.caveman-cloud/config.json (README:328); env prefix CAVEMAN_*.
- settings.json statusLine = single slot; caveman refuses foreign statusLine (install.js:1207) but plugin nudge asks Claude to write it — samewrite must not take the slot; chain or skip.
- Context: SessionStart stdout + per-turn additionalContext both concatenate; caveman's "Enforce this reply" reminder every turn (tracker:123) + ~1-1.5k tokens/turn (README:182) — a second per-turn injector doubles the tax and risks contradictory style directives; UserPromptSubmit collects only one hookSpecificOutput per hook run (tracker:378-381) so keep own hook separate.
- Names: /caveman*, /caveman:caveman-*, cavecrew-* agents, "commit"/"review"/"compress" independent modes, banner string `CAVEMAN MODE ACTIVE`, MANAGED_HOOK_BASENAMES caveman-*.js/.sh/.ps1.

BORROW CANDIDATES:
1. Shared mode parser + JSON fixture corpus with `known_failing` → src/hooks/caveman-parse.js + tests/fixtures/mode-activation/cases.json + tests/test_mode_activation_corpus.js
2. Per-session durable-off state; RESET_SOURCES {startup,clear} vs compact/resume read-through → src/hooks/caveman-config.js:380-535, caveman-activate.js:220,284-308
3. Symlink-safe, size-capped, whitelisted flag IO (0600/O_NOFOLLOW/temp+rename) → src/hooks/caveman-config.js:168-319
4. Single SKILL.md filtered to active level + shared banner, re-injected on switch → caveman-config.js:662-725, caveman-mode-tracker.js:372-376
5. settings.json JSONC read / exact-basename ownership / foreign preservation / orphan prune → bin/lib/settings.js:103-374
(also: first-complete-JSON stdin + unref + watchdog, tracker:397-426 / activate:209-282; requireSibling degrade, tracker:17-54)

Coverage: 18 files read in full (SKILL.md, parse, tracker, config, activate, statusline.sh, settings.js, plugin.json ×2, marketplace.json, 6 commands, .codex/hooks.json, rules/caveman-activate.md); README, verify_repo.py, compile.mjs, registry.json, install.js, stats.js, evals/, benchmarks/, tests read by grep/segment only; packages/ proxy/ engine/ browse/ mcp/ extension/ not opened.```
